---
all:
  vars:
    ansible_connection: ssh
    ansible_user: root
    ansible_become: true
    ansible_ssh_private_key_file: /root/.ssh/id_rsa

zookeeper:
  hosts:
    zookeeper1:
    zookeeper2:
    zookeeper3:

kafka_broker:
  hosts:
    kafka1:
    kafka2:
    kafka3:
kafka_connect:
  hosts:
    connect:

schema_registry:
  hosts:
    schema-registry:

ksqldb:
  hosts:
    ksqldb:

control_center:
  hosts:
    control-center:

#Check_Disk:
 # hosts:
  #  kafka1
  
